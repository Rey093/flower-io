# @flower-io/api-client

Auto-generated typed client for the Encore API.

Regenerate after API changes:

```bash
pnpm gen:client
```

This writes `src/client.ts`. Consumers import from `@flower-io/api-client`.
